
# pgio for postgresql 11.2 with Docker

Reffer to https://github.com/therealkevinc/pgio for pgio options

# Install notes

docker build . -t myrepo/pgio

# Connect to your database and create database pg10

docker run --network custom_net --rm --name pgio -it myrepo/pgio bash

$ ./setup.sh

$ ./rununit.sh